import BotModel from "../models/bots.js";import {updateBots} from "../app.js";export class BotController {    static createBot = async (req, res,) => {        try {            const bot = await BotModel.create(req.body);            updateBots([bot])            res.send({ data: bot });        } catch (err) {            console.log(err)        }    }    static getBots = async (_, res,) => {        try {            const bots = await BotModel.find();            res.send({ data: bots });        } catch (err) {            console.log(err)        }    }}